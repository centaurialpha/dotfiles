backend = "egl";
vsync = true;
use-damage = true;
unredir-if-possible = false;

opacity-rule = [
    "90:class_g = 'st' && FOCUSED",
    "60:class_g = 'st' && !FOCUSED"
];

blur: {
    method = "dual_kawase";
    size = 6;
    deviation = 1.0;
}
